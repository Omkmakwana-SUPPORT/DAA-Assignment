class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        cur = 0
        maxx = -100000
        for num in nums:
            cur += num
            if cur > maxx:
                maxx = cur
            if cur < 0:
                cur = 0
        return maxx
